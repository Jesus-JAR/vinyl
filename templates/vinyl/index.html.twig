<h1> {{ title }} </h1>

{# Todo: add an image of the record (comment) #}
<h2> {{parrafo|title}} </h2>
<div>
     Tracks: 
     <ul>
        
        {% for track in tracks %}
        <li>{{ track.Artist|capitalize }} - {{ track.Song }}</li>
        {% endfor %}

     </ul>
</div>

